export const metadata = {
  title: 'Squircle - Motion-Primitives',
  description:
    'A squircle is a shape intermediate between a square and a circle.',
};

import ComponentCodePreview from '@/components/component-code-preview';
import CodeBlock from '@/components/code-block';
import {InstallationCli} from '@/components/installation-cli';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/tabs';
import SquircleAppleIcon from './squircle-apple-icon';

# Squircle

A squircle is a shape intermediate between a square and a circle.

## Examples

### Squircle Apple Icon

<ComponentCodePreview
  classNameComponentContainer='min-h-0 py-24 lg:px-32 px-8'
  component={<SquircleAppleIcon />}
  filePath='app/(docs)/docs/(docWarrap)/squircle/squircle-apple-icon.tsx'
/>